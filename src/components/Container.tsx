

const Container = ({ children }) => {
    return (
        <div className="w-full h-full max-w-[1230px] px-[20px] mx-auto">
            {children}
        </div>
    );
};

export default Container;